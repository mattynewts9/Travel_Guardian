
<h1>hi</h1>

<div class="search-text"><h4>Based on your search...here is what we found</h4>
  <h2>This area has an average safety rating of ....</h2>
</div>

<div class="map-container">
  <div class="mapbox" style="width: 60%; height: 300px;"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>


<div class="hotel-containers">
 <% @hotels.each do |hotel| %>
  <%= image_tag hotel.image %>
  <div class="d-flex">
    <%= hotel.name %>
  <%= hotel.crime_rating %>
  <%= hotel.address %>
  </div>
 <% end %>

</div>
